import type { ConversionJob } from "@/lib/types";import { Download,FileCheck2,RefreshCw } from "lucide-react";
export default function DownloadCard({job,onReset}:{job:ConversionJob;onReset:()=>void}){return <div className="download-card"><span className="done-icon"><FileCheck2/></span><div><strong>Your Word file is ready</strong><p>{job.fileName}{job.analysis?` - ${job.analysis.pages} pages - ${job.analysis.kind}`:" - exact appearance preserved"}</p></div><a className="download-button" href={`/api/download/${job.id}`}><Download/> Download .docx</a><button className="again-button" onClick={onReset}><RefreshCw/> Convert another</button></div>}
