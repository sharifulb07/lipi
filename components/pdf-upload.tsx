"use client";
import { useRef,useState } from "react";import { File,UploadCloud,X } from "lucide-react";
export default function PdfUpload({file,onChange,disabled=false}:{file:File|null;onChange:(file:File|null)=>void;disabled?:boolean}){const ref=useRef<HTMLInputElement>(null),[drag,setDrag]=useState(false);function choose(next?:File){if(next)onChange(next)}return <>{!file?<button type="button" disabled={disabled} className={`dropzone ${drag?"dragging":""}`} onClick={()=>ref.current?.click()} onDragOver={e=>{e.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={e=>{e.preventDefault();setDrag(false);choose(e.dataTransfer.files[0])}}><span className="upload-icon"><UploadCloud/></span><strong>Drop your PDF here</strong><span>or <u>browse from your device</u></span><small>PDF only - Maximum 20 MB</small></button>:<div className="file-row"><span className="pdf-icon"><File/></span><div><strong>{file.name}</strong><span>{(file.size/1048576).toFixed(2)} MB - PDF document</span></div><button type="button" onClick={()=>onChange(null)} aria-label="Remove file"><X/></button></div>}<input ref={ref} hidden type="file" accept="application/pdf,.pdf" onChange={e=>choose(e.target.files?.[0])}/></>}
