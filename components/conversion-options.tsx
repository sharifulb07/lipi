"use client";
import type { ConversionOptions as Options } from "@/lib/types";
import { CONVERSION_LANGUAGES } from "@/lib/conversion-languages";
export default function ConversionOptions({value,onChange,disabled=false}:{value:Options;onChange:(value:Options)=>void;disabled?:boolean}){
 const note=value.mode==="preserve-layout"?"High-fidelity visual copy with optimized page rendering and smaller output. Text is not editable.":value.mode==="fixed-layout"?"Keeps editable text boxes plus photos, logos, and background images in their original PDF positions.":"Reference-style editable output: paragraphs and tables stay interleaved in PDF reading order with fixed table grids and measured spacing.";
 return <><div className="options"><label><span>Original text / OCR language</span><select disabled={disabled} value={value.language} onChange={e=>onChange({...value,language:e.target.value as Options["language"]})}>{CONVERSION_LANGUAGES.map(language=><option key={language.value} value={language.value}>{language.label}</option>)}</select></label><label><span>Conversion mode</span><select disabled={disabled} value={value.mode} onChange={e=>onChange({...value,mode:e.target.value as Options["mode"]})}><option value="fixed-layout">Textbox layout + editable</option><option value="editable">Structured editable (recommended)</option><option value="preserve-layout">Exact visual copy (fast)</option></select></label></div><label className="ocr-option"><input type="checkbox" checked={value.ocr} disabled={disabled||value.mode==="preserve-layout"} onChange={e=>onChange({...value,ocr:e.target.checked})}/><span><strong>Use OCR for scanned pages</strong><small>Selectable PDF text always keeps its original Unicode language. For scanned pages, choose the matching language for the best OCR result.</small></span></label><p className="mode-note">{note}</p></>
}

